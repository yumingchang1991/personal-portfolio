import { createTheme } from '@mui/material/styles';

const main = createTheme({
  palette: {
    primary: {
      main: '#252525'
    }
  },
  typography: {
    fontSize: 16,
  }
});

export default main;